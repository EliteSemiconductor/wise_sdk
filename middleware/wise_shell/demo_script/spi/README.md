# How to use demo script

The demo script is written using Tera Term's TTL syntax.

1.Open two windows and ensure the SPI connections are properly set up.

2.First, select one side as the device_2. Load device_2.ttl via Control -> Macro.

3.The other side will act as the transmitter, controlling both interfaces. Load the corresponding TTL script according to the test item.
